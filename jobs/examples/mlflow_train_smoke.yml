schema_version: 1
job_id: mlflow-train-smoke
task: train
class_name: deforest
priority: 100
description: CPU-only synthetic smoke train to verify MLflow epoch-level logging. Does not read TIFF or train a real model.
params:
  smoke_train: true
  purpose: mlflow_epoch_logging_check
data:
  subset: synthetic
train:
  smoke: true
  epochs: 3
  epoch_sleep_sec: 0.2
  workers: 0
resources:
  requires_gpu: false
  cpu_train_slots: 1
  cpu_preprocess_slots: 0
  workers: 0
mlflow:
  experiment: mlsystem-deforest
  tags:
    pipeline: mlflow-smoke
    safety: lightweight
