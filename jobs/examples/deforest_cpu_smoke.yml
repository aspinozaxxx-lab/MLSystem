schema_version: 1
job_id: deforest-cpu-smoke
task: status_check
class_name: deforest
priority: 100
description: Lightweight CPU-only smoke check. Does not train, predict, copy TIFF, or build caches.
params:
  purpose: github_actions_enqueue_smoke
resources:
  requires_gpu: false
  cpu_train_slots: 0
  cpu_preprocess_slots: 0
  workers: 0
mlflow:
  experiment: mlsystem-mvp
  tags:
    pipeline: github-actions
    safety: lightweight
