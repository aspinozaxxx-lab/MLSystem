schema_version: 1
job_id: mvp-status-check
task: status_check
class_name: system
priority: 100
description: Lightweight status check job. Does not train or copy data.
params:
  note: server_mvp_smoke
mlflow:
  experiment: mlsystem-mvp
  tags:
    pipeline: server-mvp
    safety: lightweight
