name: sync-results

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: mlsystem-sync-results
  cancel-in-progress: false

jobs:
  deprecated:
    runs-on: [self-hosted, linux]
    steps:
      - name: Results sync deprecated
        run: |
          echo "sync-results is deprecated. Use Airflow REST API and MLflow/S3 artifacts for status/results."
