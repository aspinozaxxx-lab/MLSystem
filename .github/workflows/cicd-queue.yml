name: cicd-queue

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mlsystem-cicd-queue
  cancel-in-progress: false

jobs:
  deprecated:
    runs-on: [self-hosted, linux]
    steps:
      - name: Queue workflow deprecated
        run: |
          echo "cicd-queue is deprecated. Trigger experiments through Airflow DAG mlsystem_experiment_pipeline."
