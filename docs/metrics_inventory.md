# MLSystem Metrics Inventory

Р”Р°С‚Р° РёРЅРІРµРЅС‚Р°СЂРёР·Р°С†РёРё: 2026-05-08.

Р¦РµР»СЊ: Р·Р°С„РёРєСЃРёСЂРѕРІР°С‚СЊ, РіРґРµ СЃС‡РёС‚Р°СЋС‚СЃСЏ Рё Р»РѕРіРёСЂСѓСЋС‚СЃСЏ pixel/object F1, IoU Рё loss, Р° С‚Р°РєР¶Рµ РєР°РєРёРµ Р·РЅР°С‡РµРЅРёСЏ СЏРІР»СЏСЋС‚СЃСЏ source of truth РїРѕСЃР»Рµ РёСЃРїСЂР°РІР»РµРЅРёСЏ.

| РњРµС‚СЂРёРєР° | MLflow key / stage key | Р¤Р°Р№Р» | Р¤СѓРЅРєС†РёСЏ | Р­С‚Р°Рї pipeline | РќР° РєР°РєРёС… РґР°РЅРЅС‹С… СЃС‡РёС‚Р°РµС‚СЃСЏ | Р’С…РѕРґС‹ | Р¤РѕСЂРјР° С‚РµРЅР·РѕСЂРѕРІ | РљР°РЅР°Р»/РєР»Р°СЃСЃ | Threshold | Aggregation | Р“РґРµ Р»РѕРіРёСЂСѓРµС‚СЃСЏ | РџРѕРґРѕР·СЂРµРЅРёСЏ/РїСЂРѕР±Р»РµРјС‹ |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Pixel TP/FP/FN/TN | `train/pixel_tp`, `val/pixel_tp`, `counter_pixel_tp` Рё Р°РЅР°Р»РѕРіРё | `mlsystem/src/metrics/segmentation.py` | `pixel_counts_from_logits`, `PixelMetricAccumulator` | `train_model`, `evaluate_pixel_metrics`, `compute_f1` | train batches Рё С„РёРєСЃРёСЂРѕРІР°РЅРЅР°СЏ val РІС‹Р±РѕСЂРєР° | logits, GT mask | `N,C,H,W`, GT `N,1,H,W` | binary foreground; РґР»СЏ multi-class softmax channel `class_index` | default `0.5`, `metrics.threshold` | micro РїРѕ РІСЃРµРј РїРёРєСЃРµР»СЏРј РІС‹Р±РѕСЂРєРё | MLflow РІ `real_train.py`, pipeline artifacts/stage reports С‡РµСЂРµР· `experiment_stages.py` | Р Р°РЅСЊС€Рµ counts РЅРµ persisted; stage report РїРёСЃР°Р» not_available. |
| Pixel precision | `val/precision`, `metric_pixel_precision`, `val/cuttings_pixel_precision` | `mlsystem/src/metrics/segmentation.py` | `metrics_from_counts` | `train_model`, `evaluate_pixel_metrics`, `compute_f1` | validation samples | TP/FP | scalar counts | deforest/cuttings foreground | same as pixel counts | micro | MLflow, `pixel_metrics.json`, stage report | Р Р°РЅСЊС€Рµ СЃС‡РёС‚Р°Р»Р°СЃСЊ РєР°Рє mean per batch. |
| Pixel recall | `val/recall`, `metric_pixel_recall`, `val/cuttings_pixel_recall` | `mlsystem/src/metrics/segmentation.py` | `metrics_from_counts` | `train_model`, `evaluate_pixel_metrics`, `compute_f1` | validation samples | TP/FN | scalar counts | deforest/cuttings foreground | same as pixel counts | micro | MLflow, `pixel_metrics.json`, stage report | Р Р°РЅСЊС€Рµ СЃС‡РёС‚Р°Р»Р°СЃСЊ РєР°Рє mean per batch. |
| Pixel F1 / Dice | `val/pixel_f1`, `val/dice`, `val/pixel_dice`, `metric_pixel_f1`, `val/cuttings_pixel_f1` | `mlsystem/src/metrics/segmentation.py`; legacy wrapper in `mlsystem/src/metrics/pixel_metrics.py` | `metrics_from_counts`, `binary_segmentation_metrics_from_logits` | `train_model`, `evaluate_pixel_metrics`, `compute_f1` | validation samples | TP/FP/FN | scalar counts | deforest/cuttings foreground | same as pixel counts | micro: `2TP/(2TP+FP+FN)` | MLflow step=epoch; pipeline stage reports | Main bug: previous `real_train._dice_iou_precision_recall` produced per-batch F1 and `np.mean` across batches. Sparse positive support made jumps and inconsistencies likely. |
| Pixel IoU | `val/iou`, `val/pixel_iou`, `metric_pixel_iou`, `val/cuttings_pixel_iou` | `mlsystem/src/metrics/segmentation.py` | `metrics_from_counts` | `train_model`, `evaluate_pixel_metrics`, `compute_f1` | validation samples | TP/FP/FN | scalar counts | deforest/cuttings foreground | same as pixel counts | micro: `TP/(TP+FP+FN)` | MLflow, `pixel_metrics.json`, stage report | Р Р°РЅСЊС€Рµ РјРѕРі РЅРµ СЃРѕРІРїР°РґР°С‚СЊ СЃ precision/recall/F1 РёР·-Р·Р° СЂР°Р·РЅС‹С… batch means. |
| Pixel accuracy | `val/pixel_accuracy`, `metric_pixel_accuracy` | `mlsystem/src/metrics/segmentation.py` | `metrics_from_counts` | `train_model`, `evaluate_pixel_metrics` | validation samples | TP/FP/FN/TN | scalar counts | deforest/cuttings foreground | same as pixel counts | micro | MLflow, `pixel_metrics.json`, stage report if finite | Р Р°РЅСЊС€Рµ РЅРµ Р»РѕРіРёСЂРѕРІР°Р»Р°СЃСЊ reliably. |
| Train loss total | `train/loss`, `train/loss_total` | `mlsystem/src/training/losses.py`, `mlsystem/src/real_train.py` | `segmentation_loss_components`, `WeightedLossAccumulator` | `train_model` | train batches after train augmentations | logits, GT mask | `N,1,H,W` | foreground | n/a | weighted by batch sample count | MLflow step=epoch, `history.json/csv` | Р Р°РЅСЊС€Рµ `np.mean(batch_loss)`; for uneven last batch it was batch-macro, not sample-weighted. |
| Train BCE component | `train/loss_bce` | `mlsystem/src/training/losses.py` | `segmentation_loss_components` | `train_model` | train batches | logits, GT mask | `N,1,H,W` | foreground | n/a | weighted by batch sample count | MLflow, `history.json/csv` | Was not logged separately. |
| Train Dice component | `train/loss_dice` | `mlsystem/src/training/losses.py` | `segmentation_loss_components` | `train_model` | train batches | sigmoid(logits), GT mask | `N,1,H,W` | foreground | n/a | weighted by batch sample count | MLflow, `history.json/csv` | Was not logged separately. |
| Val loss total | `val/loss`, `val/loss_total` | `mlsystem/src/training/losses.py`, `mlsystem/src/real_train.py` | `segmentation_loss_components`, `WeightedLossAccumulator` | `train_model` | validation batches, `model.eval()`, `torch.no_grad()` | logits, GT mask | `N,1,H,W` | foreground | n/a | weighted by batch sample count | MLflow, `history.json/csv` | Р Р°РЅСЊС€Рµ `np.mean(batch_loss)`; not sample-weighted. |
| Val BCE component | `val/loss_bce` | `mlsystem/src/training/losses.py` | `segmentation_loss_components` | `train_model` | validation batches | logits, GT mask | `N,1,H,W` | foreground | n/a | weighted by batch sample count | MLflow, `history.json/csv` | Was not logged separately. |
| Val Dice component | `val/loss_dice` | `mlsystem/src/training/losses.py` | `segmentation_loss_components` | `train_model` | validation batches | sigmoid(logits), GT mask | `N,1,H,W` | foreground | n/a | weighted by batch sample count | MLflow, `history.json/csv` | Was not logged separately. |
| Object F1 | `val/object_f1`, `pseudolabel/object_f1`, `best_val_object_f1`, `metric_object_f1` | `mlsystem/src/object_metrics.py`, `mlsystem/src/metrics/object_metric_artifacts.py`, `mlsystem/src/pipeline/pseudolabel_pipeline.py` | `compute_object_f1`, `write_object_metrics_artifacts` | pseudolabel postprocess, `compute_f1` summary if available | vectorized prediction geometries vs GT geometries | Shapely polygons | geometry lists | foreground objects | object IoU threshold default `0.5` | one-to-one greedy descending IoU | MLflow, `object_metrics.json/csv`, stage report if available | Current `compute_f1` can be `not_available` before distinct validation vectorization. Tie sorting is now deterministic. |
| Object precision/recall | `val/object_precision`, `val/object_recall`, `pseudolabel/object_precision`, `pseudolabel/object_recall` | `mlsystem/src/object_metrics.py` | `compute_object_f1` | pseudolabel postprocess | vectorized prediction geometries vs GT geometries | Shapely polygons | geometry lists | foreground objects | object IoU threshold default `0.5` | one-to-one greedy matching | MLflow and object artifacts | Sensitive to threshold, min_area, dissolve and vectorization path. |
| Object matching matrix/debug | `object_matches.csv`, `object_metrics.json`, `samples/*/object_iou_matrix.csv` | `mlsystem/src/metrics/object_metric_artifacts.py`, `mlsystem/src/metrics/debug_dump.py` | `write_object_metrics_artifacts`, `write_epoch_debug` | pseudolabel postprocess, metrics debug | GT/pred objects | Shapely polygons | geometry lists | foreground objects | object IoU threshold default `0.5` | pairwise IoU + greedy one-to-one | artifacts | Previously no per-epoch sample-level object debug from training validation. |
| evaluate_pixel_metrics stage | `metric_pixel_*`, `counter_pixel_*`, `threshold` | `mlsystem/src/pipeline/experiment_stages.py` | `_extract_pixel_metrics`, `_write_pixel_metrics_artifacts` | pipeline/API `evaluate_pixel_metrics` | `training_result.json` last epoch metrics | persisted training result | scalars | foreground | from `val/threshold` | uses metrics from source-of-truth row | stage JSON/report, optional MLflow | Р Р°РЅСЊС€Рµ counters/threshold were not available; now extracted if current `train_model` wrote them. |
| compute_f1 stage | `metric_pixel_*`, `metric_object_*` when available | `mlsystem/src/pipeline/experiment_stages.py` | `_extract_pixel_metrics`, `_object_metric_summary` | pipeline/API `compute_f1` | `training_result.json` plus object postprocess metrics if present | persisted artifacts | scalars/geometries | foreground | pixel threshold from training; object IoU default | pixel from source-of-truth; object from vectorized artifacts | `f1_summary.json/txt`, MLflow | Object metrics remain `not_available` if validation vectorization did not produce object artifacts. |
| Synthetic smoke metrics | `val/iou`, `val/pixel_f1` synthetic | `mlsystem/src/debug/smoke_train.py` | `run_smoke_train` | smoke/debug only | generated synthetic curve | none | scalars | n/a | n/a | synthetic | MLflow smoke | Not production quality signal. |

## Validation data invariants

- `real_train` uses deterministic `_split_train_val_matches(..., seed=seed)` unless a prepared dataset manifest exists.
- Train batches are shuffled; validation batches are not shuffled.
- Train augmentations are applied only in the train loop.
- Validation uses `model.eval()` and `torch.no_grad()`.
- Validation metrics now reset accumulators each epoch.
- Source-of-truth pixel metrics are micro metrics from accumulated TP/FP/FN/TN over all validation pixels.
- The threshold is logged as `metrics.threshold` and `val/threshold`.

## РћСЃРЅРѕРІРЅР°СЏ РЅР°Р№РґРµРЅРЅР°СЏ РїСЂРѕР±Р»РµРјР°

Р”Рѕ РёСЃРїСЂР°РІР»РµРЅРёСЏ `train_model` СЃС‡РёС‚Р°Р» `dice/iou/precision/recall/f1` РЅР° РєР°Р¶РґРѕРј batch Рё Р·Р°С‚РµРј РїСѓР±Р»РёРєРѕРІР°Р» РїСЂРѕСЃС‚РѕРµ СЃСЂРµРґРЅРµРµ РїРѕ batch. Р”Р»СЏ СЂР°Р·СЂРµР¶РµРЅРЅРѕРіРѕ РєР»Р°СЃСЃР° В«РІС‹СЂСѓР±РєРёВ» СЌС‚Рѕ РЅРµСЃС‚Р°Р±РёР»СЊРЅРѕ: batch Р±РµР· positive support Рё batch СЃ РЅРµСЃРєРѕР»СЊРєРёРјРё РѕР±СЉРµРєС‚Р°РјРё РїРѕР»СѓС‡Р°СЋС‚ РѕРґРёРЅР°РєРѕРІС‹Р№ РІРµСЃ. РР·-Р·Р° СЌС‚РѕРіРѕ `val/pixel_f1`, `val/iou` Рё РїСЂРѕРёР·РІРѕРґРЅС‹Рµ Р·РЅР°С‡РµРЅРёСЏ РјРѕРіР»Рё РЅРµ СЃРѕРѕС‚РІРµС‚СЃС‚РІРѕРІР°С‚СЊ РµРґРёРЅРѕР№ confusion matrix РїРѕ РІСЃРµР№ validation РІС‹Р±РѕСЂРєРµ.
